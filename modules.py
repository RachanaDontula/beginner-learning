# this is called modules, importing one file into other file i.e., importing one module into other module

from calc import *

a = 10
b = 6

print(add(a,b))
print(sub(a,b))
print(mul(a,b))
print(div(a,b))